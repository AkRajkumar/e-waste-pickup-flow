
  # E-Waste Pickup Flow

  This is a code bundle for E-Waste Pickup Flow. The original project is available at https://www.figma.com/design/JtLJKuDiHZ6QcdMOR0WGkE/E-Waste-Pickup-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  